<Page
    x:Class="Rally.App.Views.MultiViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vlc="using:LibVLCSharp.Platforms.Windows"
    Background="{StaticResource RallyBackground}">
    <Grid Padding="12">
        <TextBlock Text="Multi-View follows the macOS tile model (max 4). Full grid lands with the Windows parity pass."
            Opacity="0.7" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" />
    </Grid>
</Page>
